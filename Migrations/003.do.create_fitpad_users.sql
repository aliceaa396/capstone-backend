CREATE TABLE fitpad_users
(
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT as IDENTITY,
  full_name TEXT NOT NULL,
  user_name TEXT NOT NULL UNIQUE,
  password TEXT NOT NULL,
  date_created TIMESTAMP DEFAULT now() NOT NULL
)